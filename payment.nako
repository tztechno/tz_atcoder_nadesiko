//abc173_a payment.nako
##############################
入力を尋ねて、入力文字列に代入。
金額は、入力文字列のINT。
お釣りなしは金額を1000で割った余り。
金額を1000で割って切り捨て、それに1を足して、それを枚数に代入
もし、お釣りなしが0ならば
　 枚数を1減らす
ここまで
枚数の1000倍から金額を引いて、それを表示
##############################
入力を尋ねて、Nに代入。
a=N%1000
b=1000-a
c=b%1000
c を表示。
##############################
入力を尋ねて、Nに代入。
aはNを1000で割った余り。
bは1000からaを引く。
cはbを1000で割った余り。
cを表示。
##############################
入力を尋ねて、Nに代入。
A=(1000-N%1000)%1000
A を表示。
##############################
